# 🚂 اجرای ربات روی Railway

ربات وب‌سرور نداره (با polling کار می‌کنه)، پس روی Railway به‌عنوان یه **worker** اجرا می‌شه و **نیازی به دامنه (Domain) نیست**.

## قبل از شروع

- یه اکانت [GitHub](https://github.com) و یه اکانت [Railway](https://railway.com)
- توکن ربات از @BotFather و آیدی عددی ادمین‌ها از @userinfobot
- Railway رایگان نیست؛ اعتبار و پلنش رو تو خود سایت چک کن (ربات سبکه و مصرف کمی داره)

## مرحله ۱ — آپلود روی GitHub

1. تو GitHub یه ریپوی **Private** بساز (New repository).
2. زیپ رو **از حالت فشرده دربیار** و **فایل‌های داخل پوشه‌ی `TruthDareBot`** رو آپلود کن (Add file ← Upload files).
   خود فایل zip رو آپلود نکن؛ Railway نمی‌تونه ازش استفاده کنه.
3. توکن رو **داخل فایل‌ها ننویس**. توکن رو تو مرحله‌ی ۳ تو Railway می‌ذاری.

## مرحله ۲ — ساخت پروژه

Railway ← **New Project** ← **Deploy from GitHub repo** ← ریپوت رو انتخاب کن.
Railway خودش پایتون رو تشخیص می‌ده، `requirements.txt` رو نصب می‌کنه و ربات رو با `Procfile` بالا میاره.

## مرحله ۳ — Variables (توکن و ادمین‌ها)

روی سرویس بزن ← تب **Variables** ← این دو تا رو اضافه کن:

| اسم | مقدار |
|---|---|
| `BOT_TOKEN` | توکنی که از BotFather گرفتی |
| `ADMINS` | آیدی عددی ادمین‌ها، با کاما جدا. مثلا `111111111,222222222` |

## مرحله ۴ — Volume (خیلی مهم ⚠️)

فایل سیستم Railway **موقت** هست؛ اگه Volume وصل نکنی، با هر دیپلوی جدید **سوال‌ها و امتیازها پاک می‌شن**.

1. تو صفحه‌ی پروژه راست‌کلیک کن (یا `⌘K` / `Ctrl+K`) ← **Volume** ← وصلش کن به سرویس ربات.
2. **Mount path** رو بذار `/data`.

همین‌قدر کافیه: ربات خودش مسیر Volume رو پیدا می‌کنه و دیتابیس رو اونجا (`/data/td.db`) می‌سازه.
(اگه خواستی مسیر دلخواه بدی، متغیر `DB_PATH` رو تنظیم کن.)

## مرحله ۵ — بررسی

سرویس رو Deploy کن، بعد تب **Logs** رو باز کن. باید ببینی:

```
Bot is running...
```

بعد تو تلگرام به ربات پیام بده. برای گروه هم یادت نره `/setprivacy` ← **Disable** رو تو @BotFather بزنی (راهنما تو README.md).

## ⚠️ نکته‌های مهم

- **فقط یه نسخه از ربات باید روشن باشه.** اگه همین ربات (با همین توکن) رو الان رو Termux هم اجرا کردی، اونو ببند؛ وگرنه تلگرام خطای `409 Conflict` می‌ده.
- با هر بار Push به GitHub، Railway خودش دوباره دیپلوی می‌کنه و چون Volume داری، داده‌ها می‌مونن.
- تغییر توکن/ادمین‌ها: فقط Variables رو عوض کن (سرویس خودش دوباره بالا میاد).

## 💾 بکاپ

تو ربات: پنل ادمین ← «💾 بکاپ دیتابیس»؛ فایل `td.db` مستقیم تو تلگرام برات فرستاده می‌شه. هر چند وقت یه بار بگیر.

## 📦 (اختیاری) انتقال دیتابیس قبلی از Termux

اگه رو Termux سوال و امتیاز داشتی و می‌خوای منتقل کنی، با Railway CLI (طبق مستندات خود Railway) فایل `td.db` رو تو ریشه‌ی Volume آپلود کن:

```bash
railway volume files upload ./td.db /td.db
```

اگه از صفر شروع می‌کنی، این مرحله رو رد کن.
